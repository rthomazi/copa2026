name: Copa Export
on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install requests
      - run: python .github/scripts/export.py
        env:
          FIGMA_TOKEN: ${{ figd_aD9AKZrWOPSSjPvA_GM8GGXBZhrLPFDyFSi_uoQL }}
          GH_TOKEN: ${{ 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 }}
